# AgriSense — Mini Project
All code lives in "Mini Project/" only.
.py files for training and serving. Notebooks for EDA only.
Commit after every task. Never import from notebooks.
Execution order: preprocess → train_crop → train_yield →
train_disease → app.py